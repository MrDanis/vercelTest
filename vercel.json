{
    "version": 2,
    "env": {
    "PORT": "3000",
    "API_KEY": "your-api-key"
     },
    "builds": [
      {
        "src": "server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js"
      }
    ]
  }